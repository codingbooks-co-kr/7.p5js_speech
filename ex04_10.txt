// 원의 중심에서 마우스와의 거리에 따라 원의 색상 변경

let r = 100.0;
function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  let d = distance();
  console.log(d);
}

function distance() {
  let dd = dist(width/2, height/2, mouseX, mouseY);
  fill(dd, dd, 255);
  ellipse(width/2, height/2, r*2, r*2);
  return dd;
}